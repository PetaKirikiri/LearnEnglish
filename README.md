# Fifa English

An English reading app built with the same core stack and configuration as the Connectors project: React, TypeScript, Vite, Tailwind CSS, Vitest, and ESLint.

## Start locally

```sh
npm install
npm run dev
```

The initial reading is **A Train above You**.
